# Replicate API Configuration
# Get your API token from: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=your_replicate_api_token_here

# Cloudflare R2 Storage Configuration
# Get from: https://dash.cloudflare.com -> R2 Object Storage
STORAGE_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
STORAGE_REGION=auto
STORAGE_ACCESS_KEY=your_r2_access_key_id  
STORAGE_SECRET_KEY=your_r2_secret_access_key
STORAGE_BUCKET=your_r2_bucket_name
STORAGE_DOMAIN=your_custom_domain.com

# Legacy Cloudflare R2 Configuration (for backward compatibility)
# CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id
# CLOUDFLARE_R2_BUCKET=your_r2_bucket_name
# CLOUDFLARE_R2_ACCESS_KEY_ID=your_r2_access_key_id  
# CLOUDFLARE_R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
# CLOUDFLARE_R2_PUBLIC_DOMAIN=files.yourdomain.com

# Next.js Configuration
NEXT_PUBLIC_WEB_URL=http://localhost:3000

# Optional: YouTube API Key (for enhanced features)
# YOUTUBE_API_KEY=your_youtube_api_key_here

# File Upload Configuration
MAX_UPLOAD_SIZE_MB=500

# Cache Configuration
CACHE_CLEANUP_INTERVAL_HOURS=1
DEFAULT_CACHE_TTL_DAYS=30
# === Webhook callback domain (for local dev use ngrok https URL) ===
# CALLBACK_BASE_URL=https://your-domain-or-ngrok.example

# === Suppliers ===
# Deepgram
# DEEPGRAM_API_KEY= # 用于调用 Deepgram API
# DEEPGRAM_WEBHOOK_SECRET= # 推荐：用于验证回调签名的密钥（使用 openssl rand -hex 32 生成）
# DEEPGRAM_REQUIRE_SIGNATURE=true # 可选：严格模式，要求必须有签名（生产环境推荐）

# Replicate
# REPLICATE_API_TOKEN=
# REPLICATE_WEBHOOK_SECRET= # optional: enable signature validation in callback

# === Async supplier switch ===
# SUPPLIER_ASYNC=deepgram|replicate|both|off

# === Anonymous preview limit (shared for FE/BE) ===
# NEXT_PUBLIC_ANON_PREVIEW_DAILY_LIMIT=10
# SIMULATE_CALLBACK=true # dev only; skips signature validation for callbacks
